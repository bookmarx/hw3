<nav>
    <div class="bm-nav-top row">
        <h4 class="bm-title">Bookmarx</h4>
        <div class="right">
            <a id="bm-add" href="#" class="button in in-plus in-lg" aria-hidden="true" title="Add Bookmark" onClick="bm.bookmark.openAddModal(event)"></a>
            <a id="bm-add-folder" href="#" class="button in in-folder-plus in-lg" aria-hidden="true" title="Add Folder" onClick="bm.folder.openModal(event)"></a>
            <div class="dropdown">
                <a class="button in in-cog in-lg"></a>
                <div class="dropdown-content">
                    <a class="button button-primary"><i class="in in-upload"></i>  Import</a>
                    <a class="button button-primary" href="/v2/bm?download=true"><i class="in in-download" download></i> Export</a>
                    <a class="button button-primary" href="#" class="" onClick="bm.bookmark.openChangeModal(event)"><i class="in in-key"></i> Change Password</a>
                    <a class="button button-primary" href="/logout" class="" title="Logout"><i class="in in-exit"></i>  Logout</a>
                </div>
            </div>
        </div>
    </div>
    <div class="bm-nav-bottom row">
        <form id="bm-search-form" action="#" method="GET">
            <input id="bm-search" type="text" name="keyword" onKeyDown="if(event.keyCode==13) bm.bookmark.search(event);" placeholder="Search bookmarks">
            <button id="bm-search-button" type="button" class="button sb-button-submit in in-search in-md" onClick="bm.bookmark.search(event)"></button>
        </form>
        <form action="#" method="GET">
            <select id="bm-sort" name="orderBy">
                <% dd.forEach(function(val, key){%>
                    <option value="<%= val.value %>" <%= val.selected %>><%= val.name %></option>
                <% }); %>

            </select>
            <button type="submit" id="bm-filter-button" class="button bm-icon-button in in-equalizer in-md" onClick="bm.bookmark.filter(event)"></button>
        </form>
    </div>
</nav>
